import { InputRegexpDirective } from './input-regexp.directive';

describe('InputRegexpDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRegexpDirective();
    expect(directive).toBeTruthy();
  });
});
